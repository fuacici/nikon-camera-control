<Window x:Class="CameraControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:myclass="clr-namespace:CameraControl.Classes"
        Title="MainWindow" Height="473" Width="1078" Loaded="Window_Loaded" xmlns:my="clr-namespace:CameraControl">
    <Window.DataContext>
        <myclass:WIAManager/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="161" />
            <ColumnDefinition Width="517*" />
            <ColumnDefinition Width="110" />
            <ColumnDefinition Width="268" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="54" />
            <RowDefinition Height="357*" />
            <RowDefinition Height="23" />
        </Grid.RowDefinitions>
        <my:Controler x:Name="controler1" Grid.Row="1" IsEnabled="{Binding Path=IsConected}" Grid.RowSpan="2" />
        <StackPanel Grid.Column="1">
            <StackPanel  Orientation="Horizontal">
                <Label Content="Manufacturer :" />
                <Label Content="{Binding Path=Manufacturer}" />
            </StackPanel>
            <StackPanel  Orientation="Horizontal">
                <Label Content="Model :" />
                <Label Content="{Binding Path=DeviceName}" />
            </StackPanel>
        </StackPanel>
        <Button Content="Take picture" Name="button3" Click="button3_Click" IsEnabled="{Binding Path=IsConected}"/>
        <StatusBar Grid.ColumnSpan="4" Grid.Row="2" Name="statusBar1" />
        <Image Grid.Row="1" Name="image1" Stretch="Fill" Grid.Column="1" />
        <StackPanel Grid.Column="2" Name="SessionPanel" Grid.ColumnSpan="2" >
            <StackPanel.DataContext>
                <myclass:Settings/>
            </StackPanel.DataContext>
            <StackPanel  Orientation="Horizontal">
                <ComboBox  Name="cmb_session" SelectedValue="{Binding Path=DefaultSession}" ItemsSource="{Binding Path=PhotoSessions}" Width="183"  Margin="3"/>
                <Button Content="Add" Grid.Column="3" Name="btn_add_Sesion" Margin="3"/>
                <Button Content="Edit" Grid.Column="3" Name="btn_edit_Sesion" Margin="3" Click="btn_edit_Sesion_Click" />
            </StackPanel>
            <StackPanel  Orientation="Horizontal">
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
