<Window x:Class="CameraControl.windows.SettingsWnd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:myclass="clr-namespace:CameraControl"
        xmlns:themes="clr-namespace:WPF.Themes;assembly=WPF.Themes"
        themes:ThemeManager.Theme="{Binding Source={x:Static Member=myclass:ServiceProvider.Settings}, Path=CurrentTheme}"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SettingsWnd" Height="300" Width="300" Loaded="Window_Loaded" WindowStyle="ToolWindow" WindowStartupLocation="CenterScreen" Background="{DynamicResource WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="220*" />
            <RowDefinition Height="41*" />
        </Grid.RowDefinitions>
        <StackPanel >
        <StackPanel Orientation="Horizontal">
            <Label Content="Theme :"/>
                <ComboBox Name="cmb_themes" SelectedItem="{Binding Source={x:Static Member=myclass:ServiceProvider.Settings}, Path=CurrentTheme}" />
        </StackPanel>
        </StackPanel>
        <Button Content="Ok" Height="23" IsDefault="True" Name="button1" Width="75" Margin="12,6,0,12" Grid.Row="1" HorizontalAlignment="Left" Click="button1_Click" />
        <Button Content="Cancel" Height="23" IsCancel="True" Name="button2" Width="75" Margin="191,6,12,12" Grid.Row="1" Click="button2_Click" />
    </Grid>
</Window>
