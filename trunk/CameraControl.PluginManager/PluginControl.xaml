<UserControl x:Class="CameraControl.PluginManager.PluginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Plugin="clr-namespace:CameraControl.Core.Plugin;assembly=CameraControl.Core" mc:Ignorable="d" 
             d:DesignHeight="126" d:DesignWidth="516">
    <UserControl.DataContext>
        <Plugin:PluginInfo/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="379*" />
            <ColumnDefinition Width="137*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="33*" />
            <RowDefinition Height="57*" />
            <RowDefinition Height="36*" />
        </Grid.RowDefinitions>
        <Label Content="{Binding Path=Name}"/>
        <Label Content="{Binding Path=Version}" Grid.Column="1" HorizontalContentAlignment="Right"/>
    </Grid>
</UserControl>
